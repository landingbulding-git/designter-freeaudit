{
  "name": "designtér-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "cd apps/main-website && npm install && npm run build"
  },
  "devDependencies": {
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
